name: application_repository
description: A repository that manages application data.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.3
  path_provider: ^2.1.3
  rxdart: ^0.27.7
  storage:
    path: ../storage/storage

  u_search_api:
    path: ../../api

dev_dependencies:
  coverage: ^1.3.2
  mocktail: ^1.0.2
  test: ^1.21.4
  very_good_analysis: ^5.1.0
