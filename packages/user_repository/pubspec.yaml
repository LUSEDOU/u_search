name: user_repository
description: A repository that manages user data.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  authentication_client:
    path: ../authentication_client/authentication_client
  equatable: ^2.0.3
  package_info_client:
    path: ../package_info_client
  storage:
    path: ../storage/storage

dev_dependencies:
  coverage: ^1.3.2
  mocktail: ^1.0.2
  test: ^1.21.4
  very_good_analysis: ^5.1.0
