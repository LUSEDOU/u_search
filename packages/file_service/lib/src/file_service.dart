/// {@template file_service}
/// A Very Good Project created by Very Good CLI.
/// {@endtemplate}
class FileService {
  /// {@macro file_service}
  const FileService();

  /// Opens a file at the given [path].
  Future<void> openFile(String path, String title) {
    throw UnimplementedError();
  }
}
